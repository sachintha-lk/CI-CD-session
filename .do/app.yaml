name: spring-boot-demo
region: nyc
services:
- name: spring-boot-service
  image:
    registry_type: DOCR
    repository: spring-boot-demo
    tag: latest # You might want to update this dynamically in CI/CD
  http_port: 8080
  instance_count: 1
  instance_size_slug: basic-xxs
  routes:
  - path: /
